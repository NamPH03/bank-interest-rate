rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Internal personal system: all read/write handled through Cloud Functions & Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
